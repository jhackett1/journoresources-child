Journoresources Child
=====================

Wordpress theme for Journoresources. Child theme of Interface.

Making changes
--------------

This repo uses `gulp` to compile SASS into CSS. First run `npm install`, and watch for changes in the `/sass` folder with `gulp`.